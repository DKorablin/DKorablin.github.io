source "https://rubygems.org"

# Same Jekyll/Sass versions as the GitHub Pages built-in build,
# so the site renders identically on GitHub Pages and GitLab Pages.
gem "github-pages", group: :jekyll_plugins
